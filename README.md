# 🍓 strawberry
hi

## BASICALLY:
1. Have a look around
2. Create .env file in bot/ folder and add token (GIVEN)
2. Only edit components/ folder

## TO INSTALL DEPENDENCIES RUN:
```bash
pip install requirements.txt 
```
(make sure you are in the right directory)

## TO RUN BOT:
RUN main.py

## TO ADD A FUNCTION:
    - Add a .py in COMPONENTS FOLDER
    - Copy example layout
    - Run main.py

## DISCORD PY DOCS
https://discordpy.readthedocs.io/en/stable/

## FOR GIT
# DO NOT PUSH TO MAIN (until fully tested)

### To clone
```bash
git clone repo_https
```
(use https)

### Pull main
```bash
git pull origin main
```

If you want to create new feature
### Create new branch & switch to it
```bash
git branch feature/branch_name
git checkout feature/branch_name
```

Then puh or whatever
### Add & push
```bash
git add .
git commit -m "i added stuff"
git push
```
